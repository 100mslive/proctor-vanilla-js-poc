{
  "scripts": {
    "dev": "npm run build && NODE_ENV=development vite --mode development",
    "prod": "npm run build:prod && NODE_ENV=production vite --mode production",
    "build:prod": "vite build --mode production",
    "build": "vite build --mode development"
  },
  "dependencies": {
    "@100mslive/hms-video-store": "^0.12.16"
  },
  "type": "module",
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-static-copy": "^1.0.6"
  }
}
